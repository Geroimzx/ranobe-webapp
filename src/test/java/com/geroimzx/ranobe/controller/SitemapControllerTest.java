package com.geroimzx.ranobe.controller;

import com.geroimzx.ranobe.xml.XmlUrl;
import org.aspectj.lang.annotation.Before;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;

import static org.junit.jupiter.api.Assertions.assertEquals;

class SitemapControllerTest {
/*
    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private RanobePageRepo ranobePageRepo;

    @MockBean
    private RanobeVolumeRepo ranobeVolumeRepo;

    @InjectMocks
    private SitemapController sitemapController;

    @Before(value = )
    public void init() {
        sitemapController = new SitemapController(ranobePageRepo, ranobeVolumeRepo);
    }*/

/*    @Before("")
    public void init() {
        MockitoAnnotations.initMocks(this);
    }*/

    @Test
    void main() {
       /* SitemapController sitemapController = Mockito.mock(SitemapController.class);

        XmlUrl xmlUrl = new XmlUrl("https://intery.herokuapp.com", XmlUrl.Priority.HIGH);;

        assertEquals(sitemapController.main().getXmlUrls().contains(xmlUrl), true)*/;
    }
}